# Git Workflow

- Branch: `docs/history-governance-rules`
- Remote: `origin` -> `https://github.com/anchen1011/mindex.git`
- Policy: do not push directly to `main`; prepare changes on a branch for PR submission.
- Repository-local commit identity will be used for this branch so the commit can be recorded without modifying the user's global Git configuration.
