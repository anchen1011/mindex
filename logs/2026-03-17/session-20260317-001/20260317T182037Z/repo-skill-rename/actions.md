# Actions

- Renamed the packaged repo skill directory from `mindex/assets/skills/mindex-repo/` to `mindex/assets/skills/repo/`.
- Changed the skill metadata name from `mindex repo` to `repo`.
- Updated Mindex docs, configure instructions, and tests to use the `repo` skill name.
- Re-ran unit tests and an editable-install dry-run configure validation.
